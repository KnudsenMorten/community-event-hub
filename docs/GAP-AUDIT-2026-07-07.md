# §252 Deep-dive gap audit — findings log (incremental)

## Audit 1 — timer truth table: PASS with 2 findings
All 21 timer jobs verified against documented cadence (10-min sync ✓, 1-min drain ✓, 10-min welcome
reconcile ✓, 15-min sponsor/Woo ✓, daily reminder 08:00 ✓, month-reminder correctly absent ✓).

**F1 (MEDIUM) — EnableEmailFeaturesJob fires unattended Jan 1 00:00** (`0 0 0 1 1 *`,
EnableEmailFeaturesJob.cs). Documented as admin-POST-only with a "parking" cron, but Jan 1 2027 —
5 weeks pre-event — it WILL auto-enable 12 email features for the active edition. Fix: remove the
timer trigger (month-reminder §244 pattern) and keep the admin POST invocation path.
**FIXED 2026-07-07:** hard guard at the top of `Run` — proceeds only when app setting
`EnableEmailFeatures:Allowed` == "true" (IConfiguration), else logs "guarded off …" and returns;
`[Function]`/admin-POST path kept; `masterclass-invites` dropped from its feature list (F5).

**F2 (LOW) — WaitlistOfferExpiryJob runs every 15 min for a state that cannot exist** (Offered is
reserved/unused; verified: only comparison reads, zero assignments; PromotionMode/OfferHoldHours have
a save helper but NO UI writes and NO engine reads). Fix: unschedule the trigger; doc-comment.
**FIXED 2026-07-07:** `[Function]`/`[TimerTrigger]` removed (§244 retirement pattern) with a
retirement doc-comment; class kept compiling; ROLE-FLOWS updated.

## Audit 3 — retired-but-alive: mostly clean, 2 findings
/Welcome redirect: gone ✓. CalendarFeedToken: dormant column only ✓. Readiness in NavBuilder: 1
residual reference to verify (likely comment).

**F3 (LOW) — OnlySendTo mentions survive** in Broadcast.cshtml.cs + BrevoEmailSender.cs (comments;
rings-only is the design per operator) — scrub.
**FIXED 2026-07-07:** both comments rewritten to the rings-only wording; no `OnlySendTo` mention
remains in either file.

**F4 (LOW) — month-reminder references remain** in EmailTemplateCatalog.cs + MasterClassEmailService.cs
— verify they are retirement comments/kept-render-methods only, not a live catalog entry.
**VERIFIED FIXED 2026-07-07:** EmailTemplateCatalog carries only the §244 retirement comment (no
live `masterclass-month-reminder` entry); MasterClassEmailService keeps only the render method
(`SendMonthReminderAsync`) for historic re-renders — the job itself is unscheduled (§244).

## Audit — MC email ring consistency: 2 findings
**F5 (HIGH — go-live trap) — split rings across one funnel.** Selection invite + ticket-cancellation
ride FeatureKey `welcome-email` (§241/§243); confirm/waitlist/promoted/reassignment/day-invite sends
ride `masterclass-invites` (MasterClassEmailService.cs:318,473,633,655,696,749,766,932;
MasterClassPromotionEmailService). Raising ONE ring at go-live without the other breaks the funnel
(invited but never confirmed, or confirmed-mail without invites). Fix options: (a) unify the whole MC
lifecycle on one key, or (b) document a go-live checklist rule "raise welcome-email + masterclass-invites
together". Operator to choose.
**FIXED 2026-07-07 (option a):** every MC send site (MasterClassEmailService ×8 +
MasterClassPromotionEmailService ×2) now tags FeatureKey `welcome-email` (no AttendeeWelcome tag —
the §217 cap stays welcome-only); `masterclass-invites` removed from FeatureCatalog (+resx),
EmailTemplateCatalog remapped, organizer Content tile + EnableEmailFeaturesJob list updated; tests
updated (WelcomeVariantsTests pins the whole funnel, FeatureCatalogClassificationTests,
BrevoEmailSenderRingGateTests) + new MasterClassRingUnificationTests.

**F6 (MEDIUM) — masterclass-invites sends carry ParticipantId = null** in their EmailContext
(`eventId, null, name`). The §234 participant-keyed ring fallback can't engage; an address that does
not resolve to a participant is fail-closed DROPPED. Attendees normally resolve by email, but any
provisioning race or override address silently loses confirm/waitlist mail. Fix: resolve + pass pid
like the welcome-email sends already do.
**FIXED 2026-07-07:** all MC send sites resolve the recipient's participant id by email
(`ResolveAttendeeParticipantIdAsync`, Attendee-role preferred, fail-safe null) and pass it in the
EmailContext; covered by MasterClassRingUnificationTests (resolved + unresolvable cases).

## Audit 4 — feature-flag truth: PASS with 2 findings
Zero ghost gates (no code checks a key missing from the catalog — no typo'd always-off gates).
18 of 20 low-reference flags legitimately gate via nav tags/pages.

**F7 (MEDIUM — GUI≠behavior) — `economic-erp-sync` is an inert DUPLICATE toggle.** The ERP reconcile
job actually gates on `erp-webshop-reconcile` (ErpWebshopReconcileJob.cs:73); both keys sit in the
catalog, so Settings shows two toggles where one does nothing. Fix: remove `economic-erp-sync` from
the catalog (or alias it).
**FIXED 2026-07-07:** `economic-erp-sync` removed from FeatureCatalog (+resx name/desc);
`erp-webshop-reconcile` (the key the job really gates on) stays; classification tests updated.

**F8 (MEDIUM — GUI≠behavior) — `email-resend` catalog toggle is inert.** EmailResendService (EmailLog
page resend) contains no gate/FeatureKey check; the toggle changes nothing (sends ride only the
transport ring). Fix: either gate the resend path on the flag or remove the flag.
**FIXED 2026-07-07:** EmailResendService now takes an optional FeatureGateService and checks
`IsFeatureEnabledAsync("email-resend", eventId)` before any work — off ⇒ new honest
`FeatureDisabled` outcome (nothing sent); EmailLog page surfaces it as a no-op flash
(`EmailLog.Resend.FeatureDisabled` resource); regression tests added in EmailResendTests.
