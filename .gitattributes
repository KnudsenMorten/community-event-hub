# Shell scripts must keep LF line endings, including in a Windows checkout: bash rejects a
# carriage return ("$'\r': command not found"), so scripts/*.sh would fail in Git Bash or WSL.
*.sh text eol=lf
