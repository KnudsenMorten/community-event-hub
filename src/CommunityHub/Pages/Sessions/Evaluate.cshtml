@page "/sessions/{token}/evaluate"
@model CommunityHub.Pages.Sessions.EvaluateModel
@* 🔴 RETIRED §748 — the 1–5 smiley form lived here. It now redirects to /f/{token}, the four-point
   QR feedback page, so no link still in circulation can write a row on the superseded scale.
   Nothing renders: the handler always redirects. *@
