@page
@model CommunityHub.Pages.Forms.SpeakerWizardModel
@* §148: thin redirect — OnGet sends speakers to the in-wizard stepper host (/Forms/Wizard),
   which renders each entitled step's fields inline. This body never renders; it is kept only
   so the Nav.SpeakerOnboarding entry + any /Forms/SpeakerWizard deep links stay alive. *@
