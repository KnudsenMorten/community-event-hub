@page
@model CommunityHub.Pages.Sponsor.ContactModel
@{
    ViewData["Title"] = "Contact";
    var subjectCompany = string.IsNullOrWhiteSpace(Model.CompanyName) ? "" : $" from {Model.CompanyName}";
    var mailto = $"mailto:{Model.LeadEmail}?subject=" + Uri.EscapeDataString($"[{Model.EditionCode}] Sponsor question{subjectCompany}");
}

@if (Model.AccessDenied)
{
    <div class="card">
        <h1>Contact the sponsor team</h1>
        <p class="error">This page is for sponsors only.</p>
        <p><a asp-page="/Index">Back to hub</a></p>
    </div>
    return;
}

<div class="card">
    <h1>Contact the sponsor team</h1>
    <p>Reach out for anything sponsor-related — booth setup, deliverables, billing, schedule, attendee bag content, anything that does not have its own task.</p>

    <h3 style="margin-top:18px;">Sponsor lead</h3>
    <p><strong>@Model.LeadName</strong> &mdash; <a href="@mailto" class="task-link-btn">Send email to @Model.LeadEmail</a></p>

    <h3 style="margin-top:18px;">Organizers</h3>
    <p style="line-height:1.7;">
        General email: <a href="mailto:info@expertslive.dk">info@expertslive.dk</a><br />
        Phone: <a href="tel:+4540178179">+45 40 178 179</a>
    </p>
</div>
