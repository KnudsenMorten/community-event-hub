@page "/volunteer/mytasks"
@model CommunityHub.Pages.Volunteer.MyTasksModel
@*
    SUPERSEDED (§234 UX): the volunteer "My tasks" page merged into the unified
    /volunteer/myschedule. The page model issues a PERMANENT redirect there; this view
    never renders (kept only because a Razor Page route requires a .cshtml).
*@
