@page
@model CommunityHub.Pages.Speaker.EvaluationsModel
@*
    🗑 §748.1 — deliberately NO view. OnGet always redirects to /Speaker until C10 (the speaker view
    of the four-point results) exists, and the page's only other handler streams the per-session
    evaluation QR. Markup here would be unreachable — and an unreachable page that still LOOKS built
    is how a retired surface gets revived by accident.
*@
